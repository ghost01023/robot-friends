import React from 'react';

const ClearSpace = (element) => {
    let pressArray = [];
    document.addEventListener("keydown", function() {
        let keyPressed = this.key;
        pressArray.push(keyPressed);
        function
    })
}